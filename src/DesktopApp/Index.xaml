<Window x:Class="DesktopApp.Index"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Index" 
        Height="450" 
        Width="800"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1" Name="LoginPnl">
            <Button Name="LoginBtn" Click="LoginBtn_Click" Margin="6 65">Casdoor Login</Button>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" Name="UserProfilePnl" Visibility="Collapsed">
            <Label>Username:</Label>
            <TextBlock Name="Username" Padding="6 6"></TextBlock>
            <Button Name="LogoutBtn" Click="LogoutBtn_Click" Margin="6 10">Logout</Button>
        </StackPanel>
    </Grid>
</Window>
